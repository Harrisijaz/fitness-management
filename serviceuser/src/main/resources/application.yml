spring:
  application:
    name: service-user

  datasource:
    url: jdbc:postgresql://localhost:5432/fitness_user_database
    username: postgres
    password: post123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/